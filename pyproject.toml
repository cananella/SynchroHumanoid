[project]
name = "synchrohumanoid"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = "==3.10.*"
dependencies = [
    "lerobot",
    "genesis-world>=0.3.7",
    "mani-skill>=3.0.0b21",
    "pyyaml>=6.0.3",
]


[tool.uv.sources]
lerobot = { path = "third_party/lerobot", editable = true }
